<% @deleted_ids.each do |id| %>
  <%= turbo_stream.remove "blog_#{id}" %>
<% end %>

<%= turbo_stream.replace "action_bar" do %>
  <%= render "blogs/action_bar", blogs: current_user.blogs.order(created_at: :desc) %>
<% end %>

<%= turbo_stream.replace "flash" do %>
  <%= render "shared/flash" %>
<% end %>